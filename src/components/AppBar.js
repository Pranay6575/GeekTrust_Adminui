import React from "react";
import { Typography, Box } from "@mui/material";

const AppBar = () => {
  return (
    <Box className="header">
      <Typography className="header-title">Admin-UI Panel</Typography>
    </Box>
  );
};

export default AppBar;
